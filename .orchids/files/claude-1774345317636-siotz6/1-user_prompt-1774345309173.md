Now the structure is perfectly clear:

y=180-182: Top border of first input box
y=216-218: Bottom border of first input box
The box has a vertical divider at x=970 (71.5%): fullName in left portion (11%-71%), dniNumber in right (72%-95%)
y=255-256: Top border of second input box (DNI row)
y=290-294: Bottom border of second row
Wait — but looking at the image again there's only ONE input row. The "Nom i cognoms" and "DNI/NIE/Passaport" are headers for the same row. The box at y=180-216 contains both, split by the vertical line at x=970.

Then y=255-290 is another box — that's for the "Declaració" section header text area. Let me check what's in that second box. Actually looking again at the scan results at y=234, there are LEFT clusters at 148-257 (11%-19%) and RIGHT clusters at 992-1144 (73%-84%) — those are the HEADER LABELS "Nom i cognoms" and "DNI / NIE / Passaport" that appear BELOW the input box (as labels for the second box?).

Actually looking at the original document image more carefully — the structure is:

Top: "Declaració" section header
Row with "Nom i cognoms" label (left) + "DNI / NIE / Passaport" label (right) at y≈150
Input box: top border y=180, bottom y=218, split by vertical divider at x=970
Inside box: fullName left side, dniNumber right side
Below box: "Actuant com a" and checkboxes
So fullName goes at top: 9.8% (between y=182 and y=216), left 11% to right of divider ~71%. DNI goes same top%, left 73%. That's what I had. But the debug showed them sitting on the "Declaració" title row. The mismatch must be in how top % is calculated vs the image render. Since I'm using [container-type:inline-size], the top % is relative to the container height which equals the image height. Let me recalculate:

Image is 1357×1920px
y=182/1920 = 9.48% — so top: 9.5% should sit right on the box top border
The input row center is at y=(182+216)/2 = 199 → 199/1920 = 10.4%
The issue from the debug is the container-type:inline-size — I was missing that on step 3! The current code doesn't have it. Let me check what it renders at — since the container is inline-size, cqw works but regular % for top/left is still relative to the element's positioned parent which would be the div.relative. That should be correct. Let me check if the container-type was newly added in my edit:

Looking at my edit, I did add [container-type:inline-size] to the wrapper. The debug image I ran was based on pixel positions, not cqw. The debug shows the overlays at the wrong position — let me reconsider.

Looking at the debug image: the blue bar is on the "Declaració" text which appears around y=7-8% visually. My top: 9.8% should be inside the white box. The confusion might be that the debug visualization was calculating top correctly but the rendered PNG appears different. Let me trust the math: y=199/1920=10.4% is inside the white box. Let me just update the positions based on the math: