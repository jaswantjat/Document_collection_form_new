model = "o4-mini"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"

developer_instructions = """
Stay in exploration mode.
Trace the real execution path through the Eltex codebase, cite files and line numbers, and avoid proposing fixes unless the parent agent asks.
Prefer targeted grep and file reads over broad scans.
When tracing EC status, always follow the chain: db.json → server.js route → dashboardProject.ts → Dashboard.tsx.
When tracing PDF generation, follow: energyCertificateDocument.ts → renderEnergyCertificateOverlay → pdf-lib calls.
"""
